export interface IUserData {
    authorized: boolean,
    username: string,
    tag: string,
    password?: string,
    email?: string,
    avatar?: string,
    firstName?: string,
    lastName?: string,
    about?: string,
    shortUsername?: string,
    birthDate?: Date,
}
